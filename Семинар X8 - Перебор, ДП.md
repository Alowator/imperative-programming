# Семинар X8 - Перебор, ДП

## Прелюдия: оптимальная подструктура

**Вопрос**: если кратчайший путь из A в B проходит через вершину C, что можно сказать про его участки A→C и C→B? (они тоже кратчайшие)

**Мысль**: задача хорошо решается ДП, когда оптимальное решение строится из оптимальных решений подзадач.

## Размен монет

Есть монеты номиналом 1, 3 и 4. Как разменять сумму 10, потратив минимум монет?

**Вопрос**: почему жадный алгоритм (брать максимальный номинал) не работает? (для 6: 4+1+1 = 3 монеты, а оптимум 3+3 = 2 монеты)

**Задача**: решить на доске — заполнить таблицу `dp[0..10]`, где `dp[i]` — минимум монет на сумму `i`.

**Идея**: `dp[i] = 1 + min(dp[i-1], dp[i-3], dp[i-4])`

**Задача (код)**: написать восходящий алгоритм — цикл по суммам, без рекурсии.

**Задача (код)**: написать нисходящий алгоритм — рекурсия с мемоизацией.

**Вопрос**: как восстановить ответ (какие именно монеты взяли)? (хранить в отдельном массиве, какой номинал дал минимум)

Рекурсивное решение:

![Рекурсивное решение размена монет](img/image%205.png)

## Задача о рюкзаке

Рюкзак вместимостью 6, четыре предмета со стоимостью `Vi` и весом `Wi`. Каждый предмет берём не более одного раза. Максимизировать суммарную стоимость.

**Вопрос**: какие два варианта ДП? (по уравнению Беллмана или состояние «взяли/не взяли»; плюс рекурсия с мемоизацией)

**Определение**: `dp[i][w]` — максимальная стоимость, которую можно набрать из первых `i` предметов при ограничении веса `w`.

**Идея** (Беллман):

```
dp[i][w] = max(
    dp[i-1][w],                    // НЕ берём i-й
    dp[i-1][w - wi] + vi           // берём i-й (если влезает: wi ≤ w)
)
```

**Задача**: решить на доске — построить таблицу 5×7 для конкретных `V, W`.

**Задача (код)**: написать по уравнению Беллмана.

**Вопрос**: как восстановить решение? (пройти таблицу с конца: если `dp[i][w] != dp[i-1][w]` — предмет взяли)

**Вопрос**: а если каждый предмет можно брать несколько раз? (задача сводится к размену монет — достаточно одномерного массива `dp[w]`)

**Вопрос**: какая сложность? (время и память `O(N·W)`; память можно ужать до `O(W)`, если катить одномерный массив справа налево)

Решение по уравнению Беллмана:

![Решение рюкзака по Беллману](img/image%206.png)

## Зайчик

**Задача** (Зайчик — 11 с acmp.ru). В клетке лесенка из `N` ступенек. Заяц прыгает максимум на `K` ступенек за раз. Сколько различных маршрутов наверх? Пример: `K=3, N=4` → 7 маршрутов (1+1+1+1, 1+1+2, 1+2+1, 2+1+1, 2+2, 1+3, 3+1).

**Вопрос**: какое рекуррентное соотношение? (`dp[i] = dp[i-1] + dp[i-2] + ... + dp[i-K]`)

**Вопрос**: чему равно `dp[0]`? (1 — пустой маршрут)

**Вопрос**: что при `K=2`? (числа Фибоначчи)

**Вопрос**: почему в эталонном решении `BigInteger`? (количество маршрутов растёт экспоненциально — переполняет `long` уже на умеренных `N`)

```java
BigInteger[] arr = new BigInteger[n + 1];
arr[0] = BigInteger.ONE;
for (int i = 1; i <= n; i++) {
    arr[i] = BigInteger.ZERO;
    for (int j = 1; j <= k; j++) {
        if (i - j >= 0) {
            arr[i] = arr[i].add(arr[i - j]);
        }
    }
}
```

**Вопрос**: как это сделать в C без готовой длинной арифметики? (хранить число как массив цифр, сложение в столбик)

## Финал

**Мысль**: ДП — это не отдельный алгоритм, а способ мышления: «разбей на подзадачи, запомни ответы».
