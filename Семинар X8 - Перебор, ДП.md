# Семинар X8 - Перебор, ДП

Прелюдия про кротчайший путь в графе и его подпути (они тоже кратчайшие).

### Размен монет

Есть монеты номиналом 1, 3 и 4, как разменять ими купюру номиналом в 10, потратив меньше всего монет? 

**Задача:** решить на доске

**Задача:** написать восходящий алгоритм - без рекурсии

**Задача:** написать нисходящий алгоритм - с рекурсией

Рекурсивное решение:

![image.png](image%205.png)

### Задача о рюкзаке

Дан рюкзак вместимостью 6 и 4 предмета стоимостью Vi и весом Wi, как заполнить рюкзак, чтобы стоимость всех предметов была наибольшей? Каждый предмет можно взять только один раз.

**Вопрос:** как решать (2 варианта + рекурсия)

2 варианта: уравнением Беллмана или просто хранить состояния (взяли/не взяли предмет). Еще можно решить рекурсивно с меморизацией

**Задача:** решить на доске (любой вариант)

**Задача:** написать код

**Вопрос:** как восстановить решение?

**Вопрос:** а что если каждый предмет можно брать несколько раз? (задача сводится к размену монет и можно использовать одномерный массив)

Решение уравнение Беллмана:

![image.png](image%206.png)

### Зайчик

**Задача:** (Зайчик - 11 с acmp.ru). В нашем зоопарке появился заяц. Его поместили в клетку, и чтобы ему не было скучно, директор зоопарка распорядился поставить в его клетке лесенку. Теперь наш зайчик может прыгать по лесенке вверх, перепрыгивая через ступеньки. Лестница имеет определенное количество ступенек N. Заяц может одним прыжком преодолеть не более К ступенек. Для разнообразия зайчик пытается каждый раз найти новый путь к вершине лестницы. Директору любопытно, сколько различных способов есть у зайца добраться до вершины лестницы при заданных значениях K и N. Помогите директору написать программу, которая поможет вычислить это количество. Например, если K=3 и N=4, то существуют следующие маршруты: 1+1+1+1, 1+1+2, 1+2+1, 2+1+1, 2+2, 1+3, 3+1. Т.е. при данных значениях у зайца всего 7 различных маршрутов добраться до вершины лестницы.

```java
BigInteger[] arr = new BigInteger[n + 1];
arr[0] = BigInteger.ONE;
for (int i = 1; i <= n; i++) {
    arr[i] = BigInteger.ZERO;
    for (int j = 1; j <= k; j++) {
        if (i - j >= 0) {
            arr[i] = arr[i].add(arr[i - j]);
        }
    }
}
```