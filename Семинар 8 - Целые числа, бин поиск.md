# Семинар 8 - Целые числа, бин.поиск

# Семинар 8 - Целые числа, бин.поиск

### Целые числа (1 половина)

```jsx
1 == sizeof(char) ≤ sizeof(short) ≤ sizeof(int) ≤ sizeof(long) ≤ sizeof(long long)
         >= 8           >= 16          >= 16          >= 32           >= 64
```

для точных размеров есть stdint.h

unsigned x;

**Задача**: установить бит n в единицу `x |= (1u << n);`

**Задача**: установить бит n в ноль `x &= (~(1u << n));`

**Задача**: считать значения (выделить) n бита `value = (x >> n)  & 1u;`

**Задача**: инвертировать n бит `x ^= (1u << n)`

**Задача на размышление**: компактное решето эратосфена (на динамической памяти)

**Вопрос**: можно ли использовать еще меньше памяти?

**Вопрос**: изменится ли асимптотика времени или памяти?

**Вопрос**: поможет ли найти число 10^9 простое числно?

**Задача**: посчитать количество 1 бит в числе

`for (int i = 0; i< sizeof(x) * CHAR_BIT; i++) count += (x >> n)  & 1u;'`

**Магия**: p-адические числа

`for (unsigned i = x; i > 0 i = i & (i - 1)) count ++;`

### Бинарный поиск (2 половина)

**Задача**: найти в отсортированном массиве число X

**Задача**: дан отсортированный массив длины N, найти два такие числа L и R из массива, что R - L наиболее близко к D. (решить за O(N*logN))