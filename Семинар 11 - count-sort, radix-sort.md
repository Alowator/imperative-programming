# Семинар 11 - count-sort, radix-sort

# Семинар 11 - count-sort, radix-sort

### Count-sort

До этого мы рассматривали только сортировки, где мы умеем только сравнивать элементы.

**Вопрос:** существует ли сортировка сравнениями быстрее чем O(NlogN)?

**Вопрос**: сколько вообще существует перестановок N элементов?

**Ответ**: всего перестановок N!, каждый раз мы его сокращаем вдвое - формула Стирлинга

Давайте откажемся от сравнения. Просто посчитаем сколько каких чисел у нас есть.

**Задача**: напишите сортировку подсчетом для N чисел от 0 до 100

**Вопрос:** какая асимптотика у такой сортировки?

### Radix-sort

Проблема count-sort в большом количестве бакетов.

**Например**: есть массив из 8 элементов 126 670 343 432 316 173 556 348

**Вопрос:** какого размера массив понадобится?

Тогда возникает вторая идея - давайте отсортируем каждый разряд.

### Стабильность

Стабильный результат - 670 432 **343 173** 126 316 556 348

Нестабильный результат - 670 432 **173 343** 126 316 556 348

**Вопрос**: как будем сортировать по разрядам? зная, что их всего 10?

**Вопрос**: как добиться стабильности сортировки подсчетом?

Рисуем этот момент на доске.

**? Задача**: напишите поразрядную сортировку (десятичные разряды) ?

**Вопрос**: какая асимптотика у этой сортировки?

**Extra**: нахождение majority (составляет более половины массива) элемента:

1. Проверяем каждый элемент за O(N^2)
2. Делением пополам O(NlogN) +Om(1)
3. Подсчетом O(N) +Om(M)
4. Одним счетчиком O(N) +Om(1)