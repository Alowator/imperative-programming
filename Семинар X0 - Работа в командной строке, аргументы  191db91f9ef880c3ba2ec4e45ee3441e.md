# Семинар X0 - Работа в командной строке, аргументы и коды возврата

### WSL

1. Я буду использовать WSL и вам рекомендую
2. Что такое WSL?
3. Как его установить?
4. Компилируем и запускаем Hello World
5. Запускаем VS Code + WSL

### **Задача**: Реализовать gcd(x, y)

### Коды возврата

```c
int main() {
	int x, y;
	int res =  scanf("%d%d", &x, &y);
	if (res != 2 || y == 0) {
		printf("Wrong input\n");
		// Что делаем дальше?
		// repeat
		// exit(0)
		// abort()
	}

	printf("%d\n", gcd(x, y));
	return 0;
}
```

### Аргументы командной строки

```c
int main(int argc, char **argv) {
	printf("%d\n", argc);
	for (int i = 0; i < argc; i++) {
		printf("%s\n", argv[i]);
	}
}
```

```c
int main(int argc, char **argv) {
	if (argc != 3) {
		printf("Wrong input\n");
		exit(1);
	}

	int x = atoi(argv[1]);
	int y = atoi(argv[2]);

	printf("%d\n", gcd(x, y));
	return 0;
}
```

### Алгоритм Евклида

1. x = y * p + q (0 ≤ q ≤ |b|)
2. k - gcd(x, y)
3. gcd(x, y) = gcd(y, q)

Начинаем с main и кодов ошибок.

```c
// Программа с ошибкой
int gcd(int x, int y) {
	assert(y != 0);
	int q = x % y;
	if (q == 0) return y;
	return gcd(y, q);
}
```

```c
// Без ошибки
int euclid_mod(int x, int y) {
	int q = x % y;
	if (q < 0) {
		q += y < 0 ? -y : y;
	}
	return q;
}

int gcd(int x, int y) {
	assert(y != 0);
	int q = euclid_mod(x, y);
	if (q == 0) return y;
	return gcd(y, q);
}
```

```c
int gcd(int x, int y) {
	while (y != 0) {
		int temp = x;
		x = y;
		y = euclid_mod(x /*mistake*/, y);
	}
	return x;
}
```

**Вопрос**: у нас ошибка, как будем отлаживаться?

### GDB и отладка в VS Code

```c
gcc main.c -g -o gcd
gdb --args ./gcd 14 8
break main
run
tui enable
next
step (заходит в функцию)
print x
print y
```

Запускаем отладчик в VS Code

### .vscode/task.json