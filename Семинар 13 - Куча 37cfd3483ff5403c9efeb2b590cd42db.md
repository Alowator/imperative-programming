# Семинар 13 - Куча

# Семинар 13 - Куча

### Обход деревьев

**Рисуем**: представьте, что у нас есть дерево, и нам надо его удалить

LRV - postorder (порядок удаления)

**Вопрос**: если у нас есть такой порядок, то сходу можно предложить еще два, какие?

LVR - inorder (порядок линеаризации/проекции)

VLR - preorder (топологический порядок)

**Задача**: для заданного дерева напишите каждый из порядков удаления

### Куча

Не путать с областью памяти!

**Рисуем**: кучу

**Задача**: взятие максимума из кучи

**Задача**: операция добавления элемента в кучу

**Вопрос**: как удалять из кучи?

**Вопрос**: как представить кучу на Си?

```jsx
struct node {
  int data;  char exists;};struct heap {
  struct node arr[100000];};Extra Реализация операций:Insert(X):    heap[size++] = X;            //добавляем элемент новым листом в конец массива    SiftUp(size-1);              //поскольку инвариант с отцом добавленного листа может нарушиться,                                 //просеиваем вверх от листаExtractMin():    res = heap[0];               //корень всегда имеет номер 0    swap(&heap[0], &heap[size]); //меняем корень с самым правым листом (он является последним)    size--;                      //удаляем этот лист    SiftDown(0);
```