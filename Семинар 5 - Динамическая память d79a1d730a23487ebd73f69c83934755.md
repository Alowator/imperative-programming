# Семинар 5 - Динамическая память

# Семинар 5 - Динамическая память

1. Немного про sizeof и выравнивание

`struct struct_s {char c;int x;};`

1. sizeof(arr) vs sizeof(ptr)

**Задача**: решето эратосфена на динамической памяти

`xtypedef struct Struct_s {
int size;
char *p;
} Sieve;

Sieve init_sieve(int size) {
Sieve sieve = {size, calloc(size, sizeof(char))};

```
sieve.p[0] = 1;
sieve.p[1] = 1;
for (int i = 2; i * i < size; i++) {
    if (sieve.p[i]) {
        for (int j = i * i; j < size; j += i) {
            sieve.p[j] = 1;
        }
    }
}

return sieve;
```

}

char is_prime(Sieve *sieve, int x) {
return !sieve->p[x];
}

void free_sieve(Sieve *sieve) {
free(sieve->p);
}

void solve() {
Sieve sieve = init_sieve(100);
printf(“%d”, is_prime(&sieve, 7));
free_sieve(&sieve);
}`

Задача: стек на динамической памяти

`typedef struct {`

`int *data;`

`int size;`

`int capacity;`

`} Vector;`

`Vector init_vector(int capacity) {`

`Vector vec;`

`vec.size = 0;`

`vec.capacity = capacity;`

`vec.data = (int *)malloc(vec.capacity * sizeof(int));`

`return vec;`

`}`

`void push_back(Vector *vec, int value) {`

`if (vec->size == vec->capacity) {`

`vec->capacity *= 2;`

`vec->data = (int *)realloc(vec->data, vec->capacity * sizeof(int));`

`}`

`vec->data[vec->size] = value;`

`vec->size++;`

`}`

`void pop_back(Vector *vec) {`

`if (vec->size > 0) {`

`vec->size--;`

`} else {`

`printf("Нельзя удалить элемент, вектор пуст!\n");`

`}`

`}`

`void free_vector(Vector *vec) {`

`free(vec->data);`

`vec->data = NULL;`

`vec->size = 0;`

`vec->capacity = 0;`

`}`

Extra:

Виртуальная память