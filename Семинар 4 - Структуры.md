# Семинар 4 - Структуры

Задача: написать функцию, которая проверяет простоту числа

Задача: Решето Эратосфена на доске

Берем указатель на массив, возникает проблема - размер массива живет отдельно от этого указателя. Для этого в языке С есть структуры.

**int** x = 1; - своего рода ячейка памяти с именем х и значением 1 

**struct s** { int x, y };

**struct s** t = {1, 2}; - своего рода ячейка памяти с именем t

struct s *p = &t; - Вопрос: куда указывает p ? На весь t.

t.x = 5;

(*p).x = 5;  ==  p→x = 5

Задача: **площадь треугольника**

**struct point_t { int x, y; };
struct triangle_t { struct point pts[3]; };
написать функцию
int double_square(struct triangle_t tr); - удвоенная площадь**

return |(x2 - x1)(y3 - y1) + (x3 - x1)(y2 - y1)|

Задача: struct sieve_t init_sieve(int n);

Задача: is_prime(struct sieve_t s, int x);

Задача extra: is_prime(int x) с шагом 6

Проблема такого подхода, что мы не можем освободить память. 

Extra:

1. struct Point p = {.x = 10, .y = 20};
2. struct Point {
    struct Point inner;
    int x, y;
};
3. struct Example {char c, int x; }
sizeof(struct Example)