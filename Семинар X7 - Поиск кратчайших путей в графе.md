# Семинар X7 - Поиск кратчайших путей в графе

### Алгоритм Форда-Беллмана

Разбираем Форда-Беллмана на примере

![image.png](image%204.png)

**Задача:** реализовать Форда-Беллмана для списка ребер

```c
void bellmanFord(int n) {
    for (int p = 1; p < n; p++) {
        for (int i = 0; i < edges.size; i += 3) {
            int u = edges.arr[i];
            int v = edges.arr[i + 1];
            int w = edges.arr[i + 2];
            if ( d[u] + w < d[v] ) {
                d[v] = d[u] + w;
                f[v] = u;
            }
        }
    }
}

void getPath(int a, int b) {
    int x = b;
    push(&reversedPath, b);
    while ( x != a ) {
        x = f[x];
        push(&reversedPath, x);
    }
}
```

**Вопрос:** какая асимптотика?

| Список рёбер | `O(V * E)` |
| --- | --- |
| Список смежности (array) | `O(V * (V + E))`  |
| Список смежности (hashmap) | `O(V * E)` |
| Матрица смежности | `O(V³)` |

### Алгоритм Флойда-Уоршалла

**Задача:** реализовать Флойда-Уоршалла

```c
void floyd(int n) {
    for (int k = 0; k < n; k++)
        for (int i = 0; i < n; i++)
            for (int j = 0; j < n; j++)
                if ( d[i][j] > d[i][k] + d[k][j] ) {
                    d[i][j] = d[i][k] + d[k][j];
                    next[i][j] = k;
                }
}

void getPath(int a, int b) {
    int k = next[a][b];
    if ( k == -1 ) {
        if ( path.size == 0 || path.arr[path.size - 1] != a )
            push(&path, a);
        push(&path, b);
    } else {
        getPath(a, k);
        getPath(k, b);
    }
}
```

**Вопрос:** какая асимптотика?

**Вопрос:** какая асимптотика на списках и матрице смежности?

### Алгоритм Дейкстры

**Задача:** реализовать Дейкстру

```c
void dejkstra(int s, int n) {
    for (int i = 0; i < n; i++) {
        d[i] = INF;
        vis[i] = 0;
    }
	d[s] = 0;
	for (int i = 0; i < n; i++) {
		int vertex = -1;
		for (int j = 0; j < n; j++)
			if ( !vis[j] && ( vertex == -1 || d[j] < d[vertex]))
				vertex = j;
		if ( d[vertex] == INF )
			break;

		vis[vertex] = 1;

		for (int j = 0; j < v[vertex].size; j++) {
			int to = v[vertex].arr[j].to;
			int	len = v[vertex].arr[j].weight;
			if ( d[vertex] + len < d[to]) {
				d[to] = d[vertex] + len;
				prev[to] = vertex;
			}
		}
	}
}

void getReversedPath(int s, int to) {
    destroy(&path);
    Edge e;
    for (int i = to; i != s; i = prev[i]) {
        e.to = i;
        push(&path, e);
    }
    e.to = s;
    push(&path, e);
}
```

**Вопрос:** какая асимптотика

| Наивная реализация | `O(V^2 + E)` | влияет ли +E для графов без кратных ребер? |
| --- | --- | --- |
| С кучей | `O(V*logV + E*logV)`  = `O(E*logV)` |  |