# Семинар 2 - Массивы и указатели, асимптотика

Вы вообще тяните пару без перерыва?

Еще раз, читайте файл от лектора с рекомендациями по сдаче задач.

1. .
2. Абсолютно пустое пространство.
3. **0100** - У нас появляются четыре бита. *Что это? Что это за число? Может быть так, что эти биты кодируют число миллион?* Все зависит от нашего договора.
4. Чтобы их истрактовать, нам надо договориться, из какого диапазона это числа. (имя, семантическое значение, тип (value type)). *Сколько чисел можно закодировать 4 битами?* Но это все еще не полноценный тип, мы не договорились об операциях, пусть это арифметические операции по модулю 8.
5. Тогда у нас появляется object type. Заметим, что все эти значения нигде не записаны в памяти.
6. Далее у нас может возникнуть еще одно число, число b. Тип связан с ячейкой. И если это был бы Python, то это было бы все. Однако C - язык статически типизированный, это значит, что тип связан с именем, а не значением.
7. Имя a и b навсегда принимает тип i4. И если бы это была Java, то это тоже было бы все. Но у нас язык C. В языке C значения не висят в пустоте виртуальной машины. *Где существуют объекты в языке C?*
8. В языке C значения существуют в памяти. В нашей памяти мы один байт равен одному биту. Мы их адресуем по отдельности. Значит - между ними существуют пространственные отношения.
9. У нас есть расстояние от начала нашего пространства до переменной. *Как оно называется?* Можно рассматривать это как улицу, на которой стоят дома - биты. Где дому дается номер в виде расстояния от начала улицы. *Что такое адрес? Лежит ли этот адрес где-то в памяти?* Тут всего 22 байта. *Какой размер указателя? Какой адрес у a?*
10. На самом деле никаких соотношений между ними нет, это мы их придумали.
11. … Если в программе настоящий массив, это будет выглядеть не так. *Что нужно сделать, чтобы программа выглядела вот так?*

!!! Для каждой задачи далее считаем асимптотику !!!

**Задача:** есть ли адрес у pa? Выходим к доске и пишем указатели на указатели.

**Вопрос:** Можно ли написать вот так: int **ppa = &&a ?

С точки зрения математики pa1 = pa + 1  — безумие. В действительности в адресной арифметике есть только операция взятия разности указателей. 

**Задача:** написать функцию, в которой увеличиваем значение каждого элемента в 2 раза

**Задача:** написать функцию, на вход которой приходит строка (char*) - надо заменить все пробела на нижние подчеркивания.

**Задача:** написать функцию, которая считывает из файла все символы, а выводит все, кроме цифр (0-9).

**Задача:** написать функцию, которая для каждого элемента массива B, считает сумму 5 предыдущих элементов массива A, длина обоих N

**Задача:** то же самое, только считаем сумму всех предыдущих элементов.

Если сильно много времени:

- Снова про область видимости и **время жизни** переменных